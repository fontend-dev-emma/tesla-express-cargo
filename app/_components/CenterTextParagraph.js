function CenterTextParagraph({ children }) {
  return <p className="text-primary-600 text-center leading-relaxed sm:text-left  text-[0.85rem] lg:text-[1rem]">{children}</p>;
}

export default CenterTextParagraph;
